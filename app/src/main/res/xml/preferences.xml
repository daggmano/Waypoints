<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

	<MultiSelectListPreference
		android:key="@string/pref_poi_type_list"
	    android:title="Points of Interest"
	    android:summary="Waypoints will search for these types when a pin is dropped"
	    android:entries="@array/pref_poi_list"
	    android:entryValues="@array/pref_poi_list_values"/>

	<ListPreference
		android:key="@string/pref_dist_list"
	    android:title="Distance Units"
	    android:summary="Calculations will be performed and markers displayed using the selected unit"
	    android:entries="@array/pref_dist_list"
	    android:entryValues="@array/pref_dist_list_values"/>

</PreferenceScreen>